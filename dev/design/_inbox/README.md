# _inbox

Not live `/design` slugs. Incomplete SOURCE-only folders stay here so `themes/`
only holds compile inputs.

Do not keep a finished brand here. Promote measured roles into
`themes/<slug>/chrome.json` (and `design.json` when composition is not tokens).
Screenshots and first-pass JSON belong under `/tmp/iterate-layout-design/` for
the batch, then discard.

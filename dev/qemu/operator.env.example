# Local-only QEMU operator settings. Copy to ~/.config/windows-qemu/operator.env
# and replace the placeholders before using exit-node commands.
WQ_EXIT_NODE=example-exit-node
WQ_HOST_ISP_IP=198.51.100.20
